import React from "react";

import logo from "../assets/logo.svg";

const Hero = () => (
  <div className="text-center hero my-5">
    <h1 className="mb-4">Admin Dashboard</h1>

    <p className="lead">
      View all investors, deals and investor documents from the admin dashboard
    </p>
  </div>
);

export default Hero;
