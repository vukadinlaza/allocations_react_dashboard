import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  faqTitle: { textAlign: 'center', fontSize: '2em', paddingBottom: '15px' },
  faqEntry: { marginBottom: '20px', padding: '10px 20px' },
});

export default styles;
